import styled from "styled-components";
import {
  StyledInput,
  StyledLabel,
} from "../../WaterCounter/WaterCounter.styled";

export const LableMPhForm = styled(StyledLabel)`
  padding: 10px;
  margin-top: 4px;
  font-size: 18px;
  color: #fbf4f4;
  display: block;
`;

export const InputFirstStepForm = styled(StyledInput)`
  color: #908d90;
  background: #fbf4f4;
`;

export const FormText = styled.p`
  margin-top: 12px;

  color: #fbf4f4;
  font-family: Inter;
  font-size: 18px;
  font-style: normal;
  font-weight: 400;
  line-height: 1.125;
`;
